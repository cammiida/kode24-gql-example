enum DogBreed {
  HUSKY
  SHITSU
  GOLDEN_RETRIEVER
}

type Dog {
  id: ID!
  name: String!
  breed: DogBreed!
  isGoodBoi: Boolean!
}

type DogOwner {
  id: ID!
  name: String!
  dogs: [Dog!]
}

type Query {
  dogOwner(id: ID!): DogOwner
  dogs: [Dog!]
  dog(id: ID!): Dog
}
